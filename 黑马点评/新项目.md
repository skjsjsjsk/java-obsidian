1. 微服务分为: 
	- 网关, 
	- Dubbo(下面两个服务都通过Dubbo来向外暴露接口)
	  `(设计思想: 上层业务不直接操作数据库)`
	  `(面向契约编程: 调用方依赖api模块, 不依赖server模块. 意思就是说调用方只需要知道有哪些Dubbo接口, 方法要什么参数, 返回什么DTO即可, 不用管具体实现)`
		- seckill-base(秒杀核心数据层, 负责数据库数据访问, 以及分表, 分表的规则)
		- seckill-support(外围的支撑能力, 有用户的级别认定, 支付, 风控)
	`下面这些服务去调用上面的接口`
	- seckill-service(专注业务的编排像限流, 防刷, 库存校验, 下单流程等等)
	- seckill-processor(消息消费服务, 写订单)
	- seckill-job(定时任务, 更新活动状态)
2. 使用Caffeine + Redis两级缓存
	1. 会使用refreshAf