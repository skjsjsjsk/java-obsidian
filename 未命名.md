for循环遍历集合时删除元素会有什么问题? 应该如何解决?(并发修改异常)
一个对象在堆上创建后的生命周期大概咋样?
抽象类和接口的区别?
Redis高可用策略有哪些?
OpenFeign, Feign的区别?
@Bean和@Component有什么区别?
线程池中worker基于AQS?
	为了实现一个不可重入的独占锁, 用于判断当前worker是否正在执行任务.
	在线程池中, 当我们调用 `shutdown()` 或者 回收核心线程时, 线程池要去中断空闲的线程, 而不能中断正在执行任务的线程
	而 `worker` 在执行任务前会获取这把锁, 线程池在中断线程时, 发现这把锁被持有, 就说明在执行任务, 不能被中断

	不使用可重入锁也是为了防止线程池在执行任务时, 任务代码又调用了shutdown方法, 线程可以再次获取锁, 导致误认为线程空闲, 从而错误中断
索引下推
	把where条件中的涉及索引列的部分过滤操作从server层下推到存储引擎层执行, 在扫描索引时提前完成过滤, 减少回表的次数
JavaIO流里面有哪些常用类, 常用方法有哪些?
	整体来说, 按流向分输入/输出, 按数据分字节/字符
	1. 核心基类: 字节流: `Output/InputStream`; 字符流: `Reader, Writer`
	2. 常用类: 
		1. 操作文件的: 前面加File
		2. 缓冲流: 加Buffered
		3. 字节转字符: `InputStreamReader(把字节变字符)` / `OutputStreamWriter(把字符变字节)`
		4. 序列化: 加Object(只给字节流加)
	3. 核心方法: 
		1. 读: int read()
		2. 写: void write(int)
		3. 关闭: close()
你对Harness和Loop是如何理解的
你会哪些Linux命令? lsof netstat
	top: 看负载; free-m: 看内存余量; jmap: Java堆分析; df-h: 看磁盘空间
	lsof: 查看进程打开了哪些文件或者是谁占用了端口
	netstat: 显示网络连接、路由表、接口统计信息. 
		作用: 监控网络的连接状态(如TCP状态), 排查连接数过高或连接超时 
