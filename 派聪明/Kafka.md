1. 文件上传并且合并之后, 上传服务只需要向kafka发送一个待处理的消息, 即可响应用户, 不需要等待解析和向量化, 消费服务可以按照自己的节奏从Kafka中拉取任务来处理, 实现了前后端的解耦
2. 削峰填谷的作用: 用户上传文件有随机性, 可能会在某段时间突然上传大量的文件, 如果没有消息队列, 那么这些请求压力将直接冲向后端, 容易导致服务过载或者宕机, 而 Kafka可以稳定的接受请求, 先将他们缓存在队列中, 可控的进行逐步消费, 保证稳定运行