# ChatOpenAI: 统一接口适配不同大模型
- 痛点: 不同厂商的大模型, 调用接口都不一样, 每换一个模型就要重写一次代码
- 解决: LangChain给不同的厂商模型封装了统一的调用接口 `ChatOpenAI` , 统一了调用的方式, 让开发者更注重应用逻辑而非底层细节
## LLM与ChatModel的区别
- LangChain中大模型分为两类: 
	1. LLM(大语言模型): 接收一段文本, 返回一段文本. 适合简单文本生成
	2. ChatModel(对话模型): 可以接收一系列的"对话消息", 返回一条对话消息. 比如GPT-4o等等. 适合多轮对话场景