- 项目接口发生OOM如何排查: 
	1. 发生OOM后, 首先就是保存现场, 从应用日志, JVM日志中确定OOM发生的具体时间和类型
		1. 如何看类型: 直接看异常的最后一行, 像  `java.lang.OutOfMemoryError: Java heap space`
		   那么就是 `Java heap space(Java堆空间不足, 重点)`; `Metaspace`; `Direct buffer memory; `unable to create new native thread`