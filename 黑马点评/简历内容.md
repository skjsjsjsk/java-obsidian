# 防止超卖和一人一单
- 我这个秒杀功能, 核心使用 `Redis 预扣库存 + Lua 原子校验 + Redis Stream 异步下单 + 数据库兜底`
- 防超卖: 第一层是 Lua 脚本原子判断库存并扣减 Redis 库存，第二层是数据库更新时带 `stock > 0` 条件兜底。
- 一人一单: 第一层是 Lua 里用 Redis Set 判断用户是否已经下单，第二层是异步落库时按用户加 Redisson 锁，并且再查一次数据库订单。