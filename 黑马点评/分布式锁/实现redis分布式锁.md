 #基于Redis的分布式锁实现思路：
Redis 实现分布式锁核心依赖 **`SETNX` 命令（互斥性）+ 过期时间（防死锁）**，结合 Lua 脚本保证原子性，进阶可用 Redisson 优化功能。
核心实现思路

1.获取锁：用 SET key 线程标识 NX EX 过期时间 命令，仅当 key 不存在时创建（保证互斥），同时设置过期时间（避免死锁）。
2.释放锁：先判断锁的线程标识是否为当前线程，一致则删除锁，通过 Lua 脚本将 “判断 + 删除” 合并为原子操作（防误删）。
3.进阶优化：使用 [[分布式锁-redission(推荐使用在分布式锁里面)]] 框架，支持可重入锁、锁重试、看门狗自动续期、主从一致性等高级特性


实现一人一单所做的操作

- 利用set nx ex获取锁，并设置过期时间，保存线程标示

- 释放锁时先判断线程标示是否与自己一致，一致则删除锁

- 特性：

- 利用set nx满足互斥性

- 利用set ex保证故障时锁依然能释放，避免死锁，提高安全性

- 利用Redis集群保证高可用和高并发特性

第一种误删:
![[Pasted image 20251129205952.png]]
- 当线程1获取了锁后，发生业务阻塞，可能会导致业务尚未完成，锁超时释放。此时，线程2可能有机会获取这把锁并开始执行。如果原先阻塞的线程1恢复执行并试图删除锁，可能会误删线程2持有的锁，导致锁的状态混乱。

解决办法:
- 核心逻辑：在存入锁时，放入自己线程的标识，在删除锁时，判断当前这把锁的标识是不是自己存入的，如果是，则进行删除，如果不是，则不进行删除
### 线程标识
![[Pasted image 20251129205627.png]]
![[Pasted image 20251129205722.png]]

- threadId:前缀用UUID生成,用来区分JVM的,   后面的获取线程ID,区别每一                个JVM下的不同线程

第二种(第一种进阶(极端))
![[Pasted image 20251129210439.png]]

释放逻辑:
![[Pasted image 20251129210659.png]]
- 注释掉的部分就是发生第二种误删情况的原因,未实现判断和删除操作的原子性
- 故使用了Lua脚本来实现unlock操作,满足原子性
- Lua脚本:
![[Pasted image 20251129210919.png]]
