# 后端开发面试问题：RAG + Agent 项目

标签：#后端开发 #Java #RAG #Agent #派聪明 #面试问题

## 一、项目真实性与整体架构

### 1. 你这个项目为什么要从 RAG 升级到 Agent？

- 原来的 RAG 链路具体解决不了哪些问题？
- 哪些场景必须用 Agent，哪些场景普通 RAG 就够了？
- 如果用户只是问一个简单事实，进入 Agent 会不会反而变慢？
- 你有没有做问题路由？简单问题和复杂问题怎么区分？
- Agent 引入后最大的工程代价是什么？
- 你怎么证明 Agent 不是只是包装概念？

### 2. 你说系统是 ReAct Agent 架构，具体 ReAct 循环是怎么跑的？

- Thought、Action、Observation 在你的 Java 代码里分别对应什么？
- 模型返回 `tool_calls` 后，后端怎么解析和执行？
- 工具执行结果怎么重新放回大模型上下文？
- 最多允许几轮工具调用？为什么这么设置？
- 如果模型一直调用同一个工具怎么办？
- 如果工具调用失败，Agent 是重试、换工具，还是直接返回失败？

### 3. 你项目的模块化分层是怎么设计的？

- Controller、Service、Tool Registry、LLM Client、Search Service 分别负责什么？
- Agent 执行器和普通问答服务为什么要拆开？
- 工具注册和工具执行为什么不能直接写在 Controller 里？
- 如果后续新增一个“知识库统计工具”，需要改哪些类？
- 如果后续切换大模型供应商，哪些代码不应该改？
- 你怎么避免 Agent 逻辑和检索逻辑耦合太重？

### 4. 你在这个项目里主要负责了哪些“后端开发”的工作？

- 哪些代码最能体现你的 Java 后端能力？
- 哪些部分是你自己设计的，哪些是参考开源或教程的？
- 你项目里最复杂的一段业务链路是什么？
- 你遇到过哪些线上化以后一定会暴露的问题？
- 如果面试官让你现场画架构图，你会怎么画？
- 这个项目和普通“调用大模型 API 的 Demo”有什么区别？

## 二、Java / Spring Boot / MyBatis

### 5. Spring Boot 在这个项目里的启动流程和核心 Bean 是什么？

- 你的 Agent 工具是怎么注册到 Spring 容器里的？
- `AgentToolRegistry` 为什么适合做成单例 Bean？
- 如果某个工具依赖外部服务初始化失败，项目启动应该失败还是降级？
- 你有没有遇到循环依赖问题？
- 配置项是写在 `application.yml` 里还是环境变量里？
- DeepSeek、Ollama、ES、Redis 这些配置怎么做环境隔离？

### 6. 你项目里 MyBatis 主要负责哪些数据访问？

- 文档元数据表怎么设计？
- 会话历史表怎么设计？
- 用户、组织标签、权限关系怎么落库？
- MyBatis XML 和注解方式你用的是哪一种？
- 动态查询文档列表时怎么避免 SQL 拼接风险？
- MyBatis 的一级缓存和二级缓存你有没有用？为什么？

### 7. MySQL 在这个系统里存什么，为什么不全存在 ES 或 Redis？

- MySQL、Redis、ES、MinIO 的数据边界分别是什么？
- 文档上传成功但解析失败时，MySQL 状态怎么变？
- 会话历史为什么不能只放 Redis？
- 文件原文为什么不直接存 MySQL？
- ES 里已有 chunk，MySQL 元数据被删了会发生什么？
- 如果 MySQL 和 ES 数据不一致，你怎么排查？

### 8. 数据库事务在文件上传和解析链路里怎么处理？

- 文件元数据写 MySQL 和文件写 MinIO 是否在同一个事务里？
- MinIO 上传成功但 MySQL 写失败怎么办？
- MySQL 写成功但 Kafka 消息发送失败怎么办？
- Kafka 消息发出后解析失败，状态怎么回滚？
- 你有没有用本地消息表或事务消息？
- 如果让你保证最终一致性，你会怎么设计？

### 9. MySQL 索引怎么设计？

- 文档表应该在哪些字段上建索引？
- 会话历史按 userId 和 conversationId 查询时索引怎么建？
- 组织标签过滤怎么建索引？
- 文件 MD5 是否应该建唯一索引？
- 如果文档列表分页很慢，你怎么优化？
- MySQL explain 你会看哪些字段？

## 三、Redis 缓存、状态与去重

### 10. Redis 在这个项目里具体缓存了什么？

- 生成态、短期会话、上传分片、限流配额、工具结果缓存分别是什么 Key？
- 哪些 Key 必须设置 TTL？
- 哪些数据不能放 Redis？
- Redis 宕机时，聊天功能应该完全不可用还是降级？
- Redis 缓存穿透、击穿、雪崩在这个项目里分别可能出现在哪里？
- Redis 的 Key 命名为什么要带版本号？

### 11. Agent 工具结果缓存是怎么设计的？

- 为什么只缓存 `search_knowledge` 和 `knowledge_stats`？
- 为什么不缓存 `generate_summary`？
- 工具结果缓存的 Key 怎么生成？
- 为什么 Key 里必须包含 userId？
- 为什么要把参数规范化后再 hash？
- hash 冲突怎么处理？
- 缓存 value 里存了哪些字段？
- `SearchResult` 反序列化时要注意什么？

### 12. Redis 工具并发去重怎么做？

- 为什么需要 `agent:tool-result-lock:v1:` 这类锁 Key？
- 你用的锁是 `setIfAbsent` 还是 Lua？
- 锁的 TTL 设置多久？为什么？
- 其他并发请求等待多久？
- 如果等待 800ms 还没有结果怎么办？
- 锁释放失败会有什么问题？
- 锁误删怎么避免？
- Redis 锁和 Java 本地锁相比有什么区别？

### 13. 工具缓存的一致性和过期策略怎么考虑？

- `search_knowledge` 为什么缓存 10 分钟？
- `knowledge_stats` 为什么缓存 60 秒？
- 文档更新后，旧的检索缓存会不会返回过期结果？
- 文档重新解析后是否需要主动删除工具缓存？
- 如果不主动删除，TTL 带来的最大不一致窗口是多少？
- 如果用户权限变化，工具缓存会不会越权？
- userId 放进 Key 能解决哪些问题，解决不了哪些问题？

### 14. Redis 缓存失败时怎么降级？

- 读取缓存失败时怎么办？
- 写缓存失败时怎么办？
- 解析坏缓存时怎么办？
- 获取锁失败时怎么办？
- 为什么缓存失败不能影响主流程？
- fail-open 和 fail-close 在这里怎么取舍？
- 缓存服务异常会不会导致工具重复执行？

### 15. Redis 生成态缓存怎么支持 WebSocket 断线恢复？

- `chat:generation:{id}:meta` 存什么？
- `chat:generation:{id}:content` 存什么？
- `chat:generation:{id}:refs` 存什么？
- `active_generation` 有什么作用？
- 前端刷新页面后怎么恢复正在生成的内容？
- 生成完成后 Redis 和 MySQL 谁是准数据源？
- 如果 Redis 生成态丢了，历史记录还能恢复吗？

## 四、Kafka 异步处理与可靠性

### 16. 为什么文件上传后要用 Kafka 异步解析？

- 直接在上传接口里解析文件有什么问题？
- Kafka 在上传、解析、向量化、ES 入库之间起什么作用？
- Kafka topic 怎么设计？
- 消息体里放文件内容还是文件地址？
- Kafka 消费失败后怎么重试？
- 如果解析任务很慢，会不会阻塞其他文件？

### 17. Kafka 消息如何保证不丢失？

- Producer 发送失败怎么处理？
- Consumer 消费失败后 offset 怎么提交？
- 你使用自动提交还是手动提交？
- 如果服务重启，未完成的解析任务怎么恢复？
- Kafka 至少一次投递会带来什么问题？
- 如何保证文件解析幂等？

### 18. Kafka 重复消费怎么处理？

- 同一个 fileMd5 被消费两次会发生什么？
- ES 会重复写入 chunk 吗？
- MySQL 解析状态会被覆盖吗？
- 你怎么设计幂等 Key？
- 如果同一个用户重复上传同一个文件，系统怎么处理？
- 如果两个用户上传相同 MD5 文件，权限怎么隔离？

### 19. Kafka 积压怎么排查？

- 如何判断是解析慢、向量化慢、ES 写入慢，还是消费者数量不够？
- Consumer lag 怎么看？
- 大文件集中上传时怎么限流？
- Kafka 消费者线程数怎么设置？
- 如果 DeepSeek 或 Ollama 向量化接口变慢，Kafka 会有什么表现？
- 如何避免积压导致 Redis、MySQL、ES 连锁压力？

## 五、MinIO 与大文件上传

### 20. 大文件分片上传是怎么实现的？

- 前端每个分片带哪些参数？
- 后端如何判断某个分片已经上传？
- Redis bitmap 在断点续传里怎么用？
- 分片上传完成后怎么合并到 MinIO？
- 合并过程失败怎么办？
- 用户刷新页面后怎么知道已上传哪些分片？

### 21. 为什么用 MinIO 存文件，而不是直接存本地磁盘？

- MinIO 和本地文件系统相比有什么优势？
- MinIO 的 bucket 和 objectName 怎么设计？
- 文件访问 URL 怎么生成？
- 私有文件如何防止被直接访问？
- MinIO 删除文件后 MySQL 和 ES 怎么同步？
- 如果 MinIO 不可用，上传接口怎么返回？

### 22. 文件上传链路有哪些安全问题？

- 如何限制文件大小？
- 如何限制文件类型？
- 如何防止恶意文件解析？
- 文件名里有特殊字符怎么办？
- 用户上传同名文件怎么处理？
- 下载或预览文件时怎么校验权限？

## 六、Elasticsearch / RAG 检索

### 23. ES 索引 mapping 是怎么设计的？

- chunk 文档里有哪些字段？
- `dense_vector` 字段维度是多少？
- `textContent`、`anchorText`、`fileMd5`、`chunkId` 分别有什么作用？
- 权限字段 userId、orgTag、isPublic 怎么设计？
- ES mapping 改了以后历史数据怎么处理？
- 为什么不把整个文档作为一个 ES 文档？

### 24. ES 混合检索怎么做？

- 关键词检索用 `match` 还是 `multi_match`？
- 向量检索用 `knn` 怎么写？
- 关键词分数和向量分数怎么融合？
- BM25 和向量相似度冲突时怎么排序？
- Rerank 在哪一层做？
- `topK` 和候选集大小怎么设置？

### 25. 为什么不用 Milvus / FAISS，而是用 ES？

- ES 做向量检索的优势是什么？
- ES 做向量检索的缺点是什么？
- 如果数据规模扩大 100 倍，ES 还能撑住吗？
- 什么情况下你会考虑引入 Milvus？
- ES 同时承担关键词检索和向量检索会不会互相影响？
- 京东或携程这种大规模场景下，你会怎么拆检索架构？

### 26. 检索准确率从 62% 提升到 85% 是怎么算的？

- 评测集有多少条问题？
- 准确率的定义是什么？
- 是召回准确率、答案准确率，还是引用准确率？
- 人工标注标准是什么？
- 有没有区分简单问题、多文档问题、无答案问题？
- 这个指标是否可以复现？
- 如果面试官让你现场拿数据证明，你能拿出什么？

### 27. 文档切片为什么用 512 token 和 overlap？

- 512 token 是怎么选出来的？
- overlap 大小是多少？
- chunk 太短有什么问题？
- chunk 太长有什么问题？
- 中文文档怎么按语义边界切分？
- PDF 页码和 chunk 怎么关联？
- 表格、标题、编号、代码块怎么处理？

### 28. 检索失败后怎么自我纠错？

- 什么情况下认为检索失败？
- ES 返回空结果怎么办？
- 最高分很低怎么办？
- embedding 失败怎么办？
- Agent 会不会自动改写 query？
- 改写 query 的次数怎么限制？
- 如果多次检索仍然没有结果，应该怎么回答？

## 七、Embedding / Ollama / DeepSeek API

### 29. Ollama 在项目里承担什么角色？

- Ollama 是用于本地大模型还是 embedding？
- Ollama 和 DeepSeek API 怎么分工？
- 本地模型和云端 API 的延迟差异怎么处理？
- 本地模型不可用时怎么降级？
- 为什么不用同一个模型完成所有任务？
- 如果部署到服务器上，Ollama 的资源占用怎么评估？

### 30. DeepSeek API 是怎么接入的？

- 请求体里 messages、tools、tool_choice 怎么构造？
- `tool_choice=auto` 和强制工具调用有什么区别？
- 流式返回怎么解析？
- `tool_calls` 在流式输出里如何组装完整？
- DeepSeek 返回 JSON 不合法怎么办？
- API 超时、限流、失败怎么处理？

### 31. Token 成本怎么控制？

- prompt 里哪些部分最耗 token？
- 工具描述会不会增加 token？
- 多轮 ReAct 为什么更耗 token？
- `max_tokens` 怎么设置？
- 工具结果怎么裁剪？
- 历史上下文怎么裁剪？
- 工具缓存能减少哪些成本，不能减少哪些成本？

## 八、Agent Tool / Function Calling

### 32. Tool Registry 是怎么设计的？

- 每个工具需要哪些元信息？
- name、description、parameters 分别给谁用？
- JSON Schema 如何约束参数？
- 后端为什么还要二次校验参数？
- 未注册工具名怎么处理？
- 新增工具时如何避免影响已有工具？

### 33. `search_knowledge` 工具怎么执行？

- 参数有哪些？
- `query` 为空怎么办？
- `topK` 超过上限怎么办？
- 用户权限在哪里校验？
- 工具结果怎么格式化给模型？
- 工具结果怎么生成引用映射？

### 34. `generate_summary` 工具为什么不做 Redis 结果缓存？

- 摘要工具是否会流式输出？
- 流式输出的工具为什么不适合直接缓存？
- 摘要结果是否和上下文、用户问题强相关？
- 缓存摘要会不会导致用户看到过期总结？
- 如果一定要缓存摘要，Key 应该怎么设计？
- 摘要工具失败后前端会看到什么？

### 35. Agent 工具调用有哪些安全边界？

- 为什么不能让模型直接访问任意 URL？
- 为什么不能让模型执行任意代码？
- 工具白名单怎么实现？
- 参数越权怎么拦截？
- 工具返回内容过长怎么办？
- 工具异常是否应该暴露给用户？

## 九、WebFlux / WebSocket / 流式响应

### 36. 你这里 WebFlux 具体用在哪里？

- 是全链路 WebFlux，还是只用了 WebClient？
- `Mono` 和 `Flux` 在项目里分别表示什么？
- 为什么不能说 WebFlux 一定比 Spring MVC 快？
- 哪些地方仍然是阻塞调用？
- 阻塞调用放在哪些线程里？
- 如果在 Netty event loop 里 `block()` 会发生什么？

### 37. WebSocket 流式响应怎么实现？

- 后端推送哪些事件类型？
- `chunk`、`tool_call`、`completion`、`error` 分别什么时候发送？
- 模型 token 流和工具状态流怎么合并？
- 前端如何实现打字机效果？
- 如果 WebSocket 断开，后端任务是否继续执行？
- 用户点击停止生成后，后端如何取消模型流？

### 38. 多轮工具调用时前端如何感知进度？

- 工具开始执行时推什么事件？
- 工具成功时推什么事件？
- 工具失败时推什么事件？
- 检索结果是否直接推给前端？
- 前端如何展示 Observation？
- 如何避免用户长时间看到空白页面？

### 39. WebSocket 在多实例部署下有什么问题？

- 用户连接在 A 实例，Agent 任务在 B 实例怎么办？
- 是否需要 sticky session？
- 是否需要 Redis pub/sub 或 MQ 转发消息？
- WebSocket 连接数很大时怎么扩容？
- 心跳和断线重连怎么做？
- 京东/携程这种高并发场景下你会如何改造？

## 十、权限、安全与多租户

### 40. 组织标签权限模型怎么设计？

- userId、orgTag、isPublic 分别表示什么？
- 一个用户多个组织标签怎么处理？
- 私有文档和公共文档怎么区分？
- 管理员是否能看所有文档？
- 权限变化后 ES 里的历史 chunk 怎么更新？
- 权限过滤是在 ES 前、ES 中，还是 ES 后做？

### 41. Agent 场景下为什么更容易出现越权？

- 模型是否知道用户权限？
- 工具缓存是否可能导致越权？
- 多轮对话历史里是否可能带出上一个用户的内容？
- 引用映射是否可能串会话？
- Redis Key 不带 userId 会有什么风险？
- 如何测试权限隔离是否正确？

### 42. 如何防止模型幻觉和编造引用？

- 引用编号从哪里来？
- 模型能不能自己生成文件名？
- 检索结果为空时能不能回答确定结论？
- 引用映射存在哪里？
- 历史记录里如何保存引用信息？
- 如果模型回答里引用了不存在的编号怎么办？

## 十一、性能优化与容量规划

### 43. 一次 Agent 问答的耗时主要花在哪里？

- 模型首 token 延迟是多少？
- ES 检索耗时是多少？
- Rerank 耗时是多少？
- 工具调用次数和总耗时是什么关系？
- WebSocket 流式输出能降低真实延迟还是体感延迟？
- 如何定位一次慢请求？

### 44. 如果 QPS 提升 10 倍，瓶颈最可能在哪里？

- MySQL 会先瓶颈吗？
- Redis 会先瓶颈吗？
- ES 向量检索会先瓶颈吗？
- DeepSeek API 限流会先瓶颈吗？
- Kafka 消费会不会积压？
- WebSocket 连接数会不会成为瓶颈？

### 45. 如何做限流和配额？

- 按用户限流还是按 IP 限流？
- 按请求数限流还是按 Token 限流？
- Redis 里限流 Key 怎么设计？
- 限流窗口是固定窗口还是滑动窗口？
- Token 配额不足时怎么返回？
- 如何防止一个用户占满模型 API 资源？

### 46. 如何监控这个项目？

- 你会监控哪些业务指标？
- 你会监控哪些技术指标？
- Agent 平均工具调用次数有什么意义？
- 工具缓存命中率怎么统计？
- Kafka lag 怎么监控？
- ES 查询耗时怎么监控？
- DeepSeek API 错误率怎么监控？

## 十二、故障排查与一致性

### 47. 用户说“上传成功但搜不到文档”，你怎么排查？

- MinIO 里文件是否存在？
- MySQL 解析状态是什么？
- Kafka 消息是否发送成功？
- Consumer 是否消费成功？
- embedding 是否生成成功？
- ES 是否写入 chunk？
- 权限过滤是否把结果过滤掉？

### 48. 用户说“回答引用打不开”，你怎么排查？

- 本轮引用映射是否生成？
- Redis refs 是否存在？
- MySQL 历史里是否保存引用？
- fileMd5 是否还能查到文件？
- MinIO 对象是否存在？
- 前端引用编号和后端映射是否一致？

### 49. 用户说“同一个问题重复查了很多次”，你怎么排查？

- Agent 是否重复生成相同 `tool_calls`？
- `MAX_REACT_TOOL_CALLS` 是否生效？
- 工具缓存是否命中？
- 缓存 Key 的 query 是否规范化一致？
- Redis 锁是否获取失败？
- 缓存 TTL 是否过短？

### 50. Redis 宕机时系统会发生什么？

- 生成态还能恢复吗？
- 工具缓存还能用吗？
- 限流和配额还能准确吗？
- 上传分片状态还能恢复吗？
- WebSocket 推送是否受影响？
- 哪些功能应该降级，哪些功能应该直接失败？

### 51. ES 宕机时系统会发生什么？

- Agent 是否还能回答？
- `search_knowledge` 应该返回什么 Observation？
- 是否可以降级为历史会话或模型直答？
- 如何避免模型在无检索证据时乱答？
- ES 恢复后缓存里的旧失败结果要不要清理？
- 如何告知用户知识库检索暂不可用？

## 十三、京东 / 携程岗位迁移追问

### 52. 如果把这个项目放到京东商品问答场景，你会怎么改？

- 商品详情、评论、售后规则分别怎么入库？
- 商品权限和企业文档权限有什么不同？
- 搜索召回会更依赖关键词还是向量？
- 热门商品问答如何缓存？
- 大促期间模型 API 限流怎么处理？
- 如何防止模型给出错误价格或库存？

### 53. 如果把这个项目放到携程旅游问答场景，你会怎么改？

- 酒店、机票、攻略、订单规则分别怎么建模？
- 实时价格和库存能不能进 RAG？
- Agent 调实时 API 时如何保证超时可控？
- 如何区分静态知识和实时数据？
- 用户问“能不能退票”时需要哪些工具？
- 如何防止模型回答过期政策？

### 54. 京东/携程后端更关注高并发，你这个项目如何支撑高并发？

- 哪些链路可以异步化？
- 哪些数据可以缓存？
- 哪些接口必须限流？
- WebSocket 连接如何水平扩展？
- Kafka 消费如何扩容？
- ES 检索如何扩容？
- 模型 API 成本如何控制？

### 55. 如果面试官不关注 AI，只问后端，你怎么介绍这个项目？

- 这个项目有哪些典型后端问题？
- 哪些地方体现了异步解耦？
- 哪些地方体现了缓存设计？
- 哪些地方体现了权限控制？
- 哪些地方体现了大文件处理？
- 哪些地方体现了故障降级？

## 十四、简历逐条深挖

### 56. “整合 MySQL + Redis + MinIO + Elasticsearch 多级存储体系”会怎么被追问？

- 为什么这些组件都需要？
- 每个组件存什么？
- 数据从上传到问答完整流转路径是什么？
- 多级存储之间如何保持一致？
- 哪个组件最容易成为瓶颈？
- 如果只能保留三个组件，你会怎么取舍？

### 57. “组织标签权限模型”会怎么被追问？

- 权限模型的数据表怎么设计？
- ES chunk 里权限字段怎么冗余？
- 用户组织变化后历史 chunk 怎么处理？
- Redis 缓存是否会造成权限延迟生效？
- 管理员和普通用户权限怎么区分？
- 如何写测试验证权限隔离？

### 58. “Kafka 解耦文件上传、处理、向量化与检索环节”会怎么被追问？

- Kafka 解耦的是哪些服务？
- 消息格式是什么？
- 消费失败怎么重试？
- 重复消费怎么幂等？
- 解析任务和向量化任务是否应该拆 topic？
- Kafka 积压时怎么降级？

### 59. “分片上传与断点续传”会怎么被追问？

- 分片大小怎么选？
- Redis bitmap 怎么记录上传状态？
- 分片乱序上传怎么处理？
- 合并时如何校验完整性？
- 文件 MD5 在前端算还是后端算？
- 恶意用户伪造 MD5 怎么办？

### 60. “512 token 递归切分策略”会怎么被追问？

- 递归切分的优先级是什么？
- 为什么不是 256 或 1024？
- overlap 怎么设置？
- 怎么评估切分质量？
- 表格和代码块怎么切？
- 切分策略对召回准确率有什么影响？

### 61. “准确率从 62% 提升到 85%”会怎么被追问？

- 评测集怎么构造？
- 62% 是哪个版本测出来的？
- 85% 是哪个版本测出来的？
- 有没有统计显著性？
- 有没有人工复核？
- 有没有可能是评测集过拟合？

### 62. “Tool 调用接口封装”会怎么被追问？

- 工具接口抽象长什么样？
- 工具入参怎么校验？
- 工具结果怎么序列化？
- 工具异常怎么处理？
- 工具是否支持流式输出？
- 工具缓存在哪一层做？

### 63. “DeepSeek API 的 tools/tool_choice”会怎么被追问？

- `tools` 参数长什么样？
- `tool_choice=auto` 什么时候会不听话？
- 为什么没有强制指定工具？
- 模型返回多个 tool_calls 怎么处理？
- tool_call_id 有什么作用？
- 流式 tool_calls 如何拼接完整参数？

### 64. “Spring WebFlux + WebSocket 流式响应”会怎么被追问？

- WebFlux 在这里具体是 WebClient 还是 WebFlux Server？
- WebSocket Handler 是哪种实现？
- 如何避免阻塞 WebSocket 线程？
- token chunk 怎么推送？
- 工具状态怎么推送？
- 用户取消生成怎么处理？

### 65. “Redis 缓存短期会话上下文、Agent Step 状态与工具调用结果”会怎么被追问？

- 短期会话上下文和 MySQL 历史有什么区别？
- Agent Step 状态具体存哪些字段？
- 工具调用结果缓存哪些工具？
- 工具缓存 TTL 为什么不同？
- 工具缓存如何防止越权？
- 工具缓存如何处理并发击穿？

